import { IProduct } from "../../types";

export class WishlistService {
 static async add(product: IProduct) {
  return;
 }
 static async remove(product: IProduct) {
  return;
 }
 static async adjustQuantity(product: IProduct) {
  return;
 }
 static async clear() {
  return;
 }
}