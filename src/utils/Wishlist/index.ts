import { Product } from "../../types";

export class WishlistService {
 static async add(product: Product) {
  return;
 }
 static async remove(product: Product) {
  return;
 }
 static async adjustQuantity(product: Product) {
  return;
 }
 static async clear() {
  return;
 }
}